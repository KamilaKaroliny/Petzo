// Importa o componente de header (cabeçalho)
// Isso registra o <app-header> no projeto
import './header/header.js';

// Importa o componente de footer (rodapé)
// Isso registra o <app-footer>
import './footer/footer.js';

// Importa o componente de banner (carrossel)
// Isso registra o <app-banner>
import './banner/banner.js';

// Importa o componente de seção (menu / categorias / conteúdo lateral)
// Isso registra o <app-secao>
import './secao/secao.js';